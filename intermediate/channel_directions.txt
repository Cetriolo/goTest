package main

import "fmt"

func main() {
	ch := make(chan int)
	producer(ch)

	// for value := range ch {
	// 	fmt.Println("Received value:", value)
	// }
	consumer(ch)

}

func producer(ch chan int) {
	go func(ch chan<- int) {
		for i := range 5 {
			ch <- i
		}
		close(ch)
	}(ch)
}

func consumer(ch <-chan int) {
	for value := range ch {
		fmt.Println("Received value:", value)
	}
}
 